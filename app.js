// Dependencies
const { default: axios } = require('axios');


/**
 * Get the steam id from user's profile URL
 * @param {String} profileUrl - The user's profile URL
 * @returns {String} - The user's steam id
 */
const getSteamId = async (profileUrl) => {
    try {

    } catch (err) {
        console.log(err);
    }
}


// http://api.steampowered.com/ISteamUser/ResolveVanityURL/v0001/?key=YOU-API_KEY&vanityurl=USER_NAME